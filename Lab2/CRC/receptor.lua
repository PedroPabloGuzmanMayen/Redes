function invertir_mensaje(mensaje) --Invierte el mensaje para empezar 
    return 0
end

function es_Redundante(posicion) --Verfica si es una posición redundante
    return 0
end

function decodificar_Mensaje(mensaje, datos, paridad)
    local contador_redundancia = 0

    for i = 1, #mensaje do
        print(mensaje.sub(mensaje, -i, -i)) --recorrer desde atrás
    end
    return 0
end


msg = "Hola"

print(1 ~ 1)

